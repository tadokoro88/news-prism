# Terraform
.terraform/
terraform.tfstate
terraform.tfstate.backup
crash.log
crash.*.log
*.tfplan

# Lambda build artifacts
build/
lambda.zip

# tfvars with secrets (only commit the .example)
*.tfvars
!*.tfvars.example
